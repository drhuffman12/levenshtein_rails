<%= form_for(histogram) do |f| %>
  <% if histogram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(histogram.errors.count, "error") %> prohibited this histogram from being saved:</h2>

      <ul>
      <% histogram.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hist %>
    <%= f.text_area :hist %>
  </div>

  <div class="field">
    <%= f.label :length %>
    <%= f.number_field :length %>
  </div>

  <div class="field">
    <%= f.label :word_length_id %>
    <%= f.text_field :word_length_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
