<%= form_for(word) do |f| %>
  <% if word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
      <% word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :length %>
    <%= f.number_field :length %>
  </div>

  <div class="field">
    <%= f.label :word_length_id %>
    <%= f.text_field :word_length_id %>
  </div>

  <div class="field">
    <%= f.label :histogram_id %>
    <%= f.text_field :histogram_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
