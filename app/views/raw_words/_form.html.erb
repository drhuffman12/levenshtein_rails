<%= form_for(raw_word) do |f| %>
  <% if raw_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raw_word.errors.count, "error") %> prohibited this raw_word from being saved:</h2>

      <ul>
      <% raw_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :is_test_case %>
    <%= f.text_field :is_test_case %>
  </div>

  <div class="field">
    <%= f.label :word_id %>
    <%= f.text_field :word_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
